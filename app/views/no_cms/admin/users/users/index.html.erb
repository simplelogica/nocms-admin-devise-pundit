<% content_for :second_menu do %>
  <%= render partial: 'user_listing_item', collection: @users, as: 'user', layout: 'user_listing' unless @users.blank? %>
<% end %>

<div class="content">
  <%= render 'no_cms/admin/shared/log_messages' %>
</div>
